'use strict';

visitNotesApp.directive('cloud', function($scope){
    /*var array = ['fever', 'chills', 'tuberculosis', 'hepatitis', 'hypothyroidism'];
    var newHTML = "";
    for (var i = 0; i < array.length; i++) {
        newHTML = newHTML + '<a href="#" <span style="color:red; font-size:' + (i + 1) * 50 + '%">' + array[i] + '</span></a>' + ' ';
    }
    return {
        template: newHTML,
        link: function($scope, $element, $attributes, $controller){

        }
    }*/
});